export type MarketCandle = {
    time: number;
    open: number;
    close: number;
    low: number;
    high: number;
};
export declare function generateMockMarketData(length: number, seed: number, startPrice?: number, startTime?: number): MarketCandle[];
