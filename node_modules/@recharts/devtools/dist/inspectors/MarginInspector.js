import React from 'react';
import { useChartHeight, useChartWidth, useMargin } from 'recharts';
import { SvgDimensionShower } from './utils/SvgDimensionShower.js';
import { ObjectInspector } from './generic/ObjectInspector.js';
const Overlay = () => {
    const margin = useMargin();
    const height = useChartHeight();
    const width = useChartWidth();
    if (margin == null || width == null || height == null) {
        return null;
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(SvgDimensionShower, { width: margin.left, height: height, labels: {
                background: 'Left',
                horizontal: 'useMargin.left',
            } }),
        React.createElement(SvgDimensionShower, { width: margin.right, x: width - margin.right, height: height, labels: {
                background: 'Right',
                horizontal: 'useMargin.right',
            } }),
        React.createElement(SvgDimensionShower, { width: width, height: margin.top, labels: {
                background: 'Top',
                vertical: 'useMargin.top',
            } }),
        React.createElement(SvgDimensionShower, { width: width, height: margin.bottom, y: height - margin.bottom, labels: {
                background: 'Bottom',
                vertical: 'useMargin.bottom',
            } })));
};
const Inspector = () => {
    const margin = useMargin();
    return React.createElement(ObjectInspector, { obj: margin });
};
export const MarginInspector = {
    Overlay,
    Inspector,
};
